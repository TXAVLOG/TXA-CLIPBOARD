<UserControl x:Class="TXABackupTool.Views.ComingSoonPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border Width="120" Height="120" Background="{StaticResource SurfaceContainerHighBrush}" CornerRadius="60" Margin="0,0,0,32">
                <TextBlock Text="⏳" FontSize="48" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            
            <TextBlock Text="{Binding Title}" Style="{StaticResource HeadlineDisplay}" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="{Binding Description}" Style="{StaticResource SubHeadline}" HorizontalAlignment="Center" Opacity="0.5"/>
        </StackPanel>
    </Grid>
</UserControl>
